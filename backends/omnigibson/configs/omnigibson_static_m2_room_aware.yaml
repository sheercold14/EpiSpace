schema_version: omnigibson_episode_recipe.v1
recipe_id: omnigibson_static_m2_room_aware
seed: 17

source:
  scene_model: Rs_int
  scene_instance: null
  source_version: behavior-1k-v3.9.0
  dataset_name: behavior-1k-assets
  load_mode: full
  floor: 0

trajectory:
  sampling_strategy: room_aware
  outbound_view_count: 6
  candidate_count: 64
  minimum_distance_m: 4.0
  fallback_minimum_distance_m: 1.5
  maximum_distance_m: 12.0
  camera_height_m: 1.5

sensor:
  width_px: 1024
  height_px: 1024
  horizontal_fov_deg: 90.0
  near_m: 0.05
  far_m: 30.0
  minimum_instance_pixels: 256
  render_warmup_frames: 4
  high_quality_rendering: true

channels:
  model_visible: [rgb]
  supervision: [depth_m, instance_id, semantic_id, world_from_camera]
  oracle_only: [scene_snapshot, scene_ir, relation_oracle]
