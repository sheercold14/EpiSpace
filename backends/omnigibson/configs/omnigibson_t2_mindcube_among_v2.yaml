schema_version: omnigibson_episode_recipe.v1
recipe_id: omnigibson_t2_mindcube_among_v2_hall_conference_large_seed31
seed: 31
source:
  dataset_name: behavior-1k-assets
  source_version: behavior-1k-v3.9.0
  scene_model: hall_conference_large
  scene_instance: null
  load_mode: structure_only
  floor: 0
trajectory:
  trajectory_class: T2
  sampling_strategy: procedural_among5
  outbound_view_count: 4
  candidate_count: 64
  minimum_distance_m: 0.5
  fallback_minimum_distance_m: 0.5
  maximum_distance_m: 4.0
  camera_height_m: 1.0
  minimum_wall_clearance_m: 1.0
among5:
  protocol_version: omnigibson_mindcube_among_layout.v2
  visibility_contract: central_plus_one_partial
  layout_id: hall_conference_large.seed31.identity.p0
  layout_radius_m: 1.6
  # Stay inside the satellite ring (the defining partial-evidence geometry),
  # while leaving enough distance for both the foreground anchor and tall
  # satellites to fit cleanly in frame.
  camera_radius_m: 1.0
  layout_yaw_deg: 0.0
  mirrored: false
  # The four camera stations remain orthogonal, but each is offset 20 degrees
  # so that the satellite behind the anchor is partially visible rather than
  # perfectly hidden by it.
  camera_azimuth_deg: [340.0, 70.0, 160.0, 250.0]
  camera_pitch_down_deg: 25.0
  center_rank: 0
  minimum_object_gap_m: 0.25
  minimum_camera_wall_margin_m: 0.25
  satellite_order:
    - among_chair
    - among_backpack
    - among_trash_can
    - among_plant
  assets:
    - name: among_anchor_footstool
      category: footstool
      model: mysfnz
      role: anchor
      bbox_size_m: [0.408, 0.406, 0.358]
    - name: among_chair
      category: straight_chair
      model: mgzcic
      role: satellite
      bbox_size_m: [0.636247, 0.590042, 0.677532]
    - name: among_backpack
      category: backpack
      model: gvbiwl
      role: satellite
      bbox_size_m: [0.488902, 0.400971, 0.474706]
    - name: among_trash_can
      category: public_trash_can
      model: gsgutn
      role: satellite
      bbox_size_m: [0.520634, 0.520634, 0.692911]
    - name: among_plant
      category: pot_plant
      model: cecdwu
      role: satellite
      bbox_size_m: [0.487892, 0.678218, 0.673789]
sensor:
  width_px: 1024
  height_px: 1024
  horizontal_fov_deg: 65.0
  near_m: 0.05
  far_m: 30.0
  minimum_instance_pixels: 128
  render_warmup_frames: 4
  high_quality_rendering: true
